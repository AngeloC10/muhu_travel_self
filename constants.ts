
import { ThemeConfig } from './types';

export const APP_NAME = "Muhu Travel";

export const DEFAULT_THEME: ThemeConfig = {
  primaryColor: 'bg-indigo-600 hover:bg-indigo-700 text-white',
  secondaryColor: 'bg-amber-500 hover:bg-amber-600 text-white',
  sidebarColor: 'bg-slate-900 text-slate-100',
};
